<!DOCTYPE html>
<html>
  <head>
    <title>Travel Hack</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Travel Hack</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/destinations">Destinations</a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Account
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if logged_in? %>
            <a class="dropdown-item" href="/users/<%= "#{current_user.id}" %>" method: :get>My Profile</a>
            <a class="dropdown-item" href="/logout" method: :post>Logout</a>
          <% else %>
            <a class="dropdown-item" href="/login" method: :get>Login</a>
            <a class="dropdown-item" href="/signup" method: :get>Create Account</a>
          <% end %>
        </div>
      </li>
    </ul>

  <%= form_tag(search_results_path, method: :get, class: 'form-inline my-2 my-lg-0') do %>
      <%= text_field_tag(:search, params[:search], class: 'form-control mr-sm-2', placeholder: "Search") %>
      <%= submit_tag("Search", class: "btn btn-outline-success my-2 my-sm-0") %>
  <% end %>


    
  </div>
</nav>
    <main>
    <%= yield %>
    </main>
  </body>
</html>
